"use client";

import { Typography } from "@mui/material";
import React from "react";

export default function Orders() {
  return (
    <React.Fragment>
      <Typography paragraph>
        Cadastro, edição e exclusão de produtos. Listagem de produtos com
        filtros (categoria, preço, estoque). Upload de imagens de produtos.
      </Typography>
    </React.Fragment>
  );
}
